import {
  Form,
  Select,
  InputNumber,
  DatePicker,
  Switch,
  Slider,
  Button
} from 'antd'

import CustomLayout from './CustomLayout'

const FormItem = Form.Item
const Option = Select.Option

export default () => (
	<CustomLayout/>
)
